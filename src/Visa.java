public class Visa {
	/* visaCreditCard() method - calculates the interest for a Visa credit card
	 * @param: balance, a double
	 * @return: the interest of the card
	 * Precondition: balance must be positive
	 */
	public static double getVisa(double balance) {
		return (balance * .1);
	}
}