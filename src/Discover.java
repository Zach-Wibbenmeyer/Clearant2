public class Discover {
	/* discoverCreditCard() method - calculates the interest for a Discover credit card
	 * @param: balance, a double
	 * @return: the interest of the card
	 * Precondition: balance must be positive
	 */
	public static double getDiscover(double balance) {
		return (balance * .01);
	}
}