public class MasterCard {
	/* masterCardCreditCard() method - calculates the interest for a MasterCard credit card
	 * @param: balance, a double
	 * @return: the interest of the card
	 * Precondition: balance must be positive
	 */
	public static double getMasterCard(double balance) {
		return (balance * .05);
	}
}